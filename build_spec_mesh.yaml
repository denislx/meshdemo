version: 0.1             
component: build
timeoutInSeconds: 5000
shell: bash
env:
  exportedVariables:
steps:
  - type: Command
    command: | 
      oci ce cluster create-kubeconfig --cluster-id ocid1.cluster.oc1.eu-amsterdam-1.aaaaaaaavc2hy67ng5aiotgium73omr5hjhvjanmw7mwel2e6cxqmdf6qtcq --file kconfig --region eu-amsterdam-1 --token-version 2.0.0  --kube-endpoint PUBLIC_ENDPOINT
  - type: Command
    command: |
      chmod +x ./kubectl
      export KUBECONFIG="kconfig"
      .export meshdemons-version=meshdemons-v8
      ./kubectl create namespace $meshdemons-version
      ./kubectl label namespace $meshdemons-version servicemesh.oci.oracle.com/sidecar-injection=enabled
      ./kubectl create secret -n $meshdemons-version docker-registry amsocirsecret --docker-username 'frsxwtjslf35/oracleidentitycloudservice/mika.rinne@oracle.com'  --docker-password 'Y}SMSjCO02X5o){e.(-K'  --docker-server 'ams.ocir.io'
      sed -i 's/meshdemons-version/${meshdemons-version}/g' app.yaml
      ./kubectl create -f app.yaml
      sleep 120
      sed -i 's/meshdemons-version/${meshdemons-version}/g' meshify-app.yaml
      sed -i 's/meshdemons-version/${meshdemons-version}/g' bind-app.yaml
      ./kubectl create -f meshify-app.yaml
      ./kubectl create -f bind-app.yaml
      